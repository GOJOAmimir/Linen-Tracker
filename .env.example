# ── Frontend (Vite) ─────────────────────────────────────────────
# Used at build time — prefix with VITE_ to expose to the browser
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:3001
